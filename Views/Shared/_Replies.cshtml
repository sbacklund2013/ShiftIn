@model IEnumerable<ShiftIn.Models.ForumPost>
@if (Model.Count() > 0)
{
    <div class="Replies m-3">
        @foreach (var item in Model)
        {
            @await Html.PartialAsync("_ForumPost", item)

        }
    </div>

}



